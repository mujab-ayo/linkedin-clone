import style from "styled-components";

const Login = (props) => {
  return (
    <div>
      <nav>
        <a href="">
          <img src="/images/login-logo.svg" alt="" />
        </a>
      </nav>
    </div>
  );
};

export default Login;
